# ADR-NNNN — <The decision, stated as a decision — not the topic>

- **Status:** proposed | accepted | superseded by ADR-XXXX
- **Date:** YYYY-MM-DD
- **Feature:** F<N>-<n>
- **Extends:** [ADR-NNNN](NNNN-....md) *(optional)*
- **Spec:** `docs/specs/YYYY-MM-DD-<topic>.md` *(optional)*

## Context

What is the problem? What forces are at play — technical, product, organizational? What constraints
exist? Where a prior art solves this differently, say what it does and what that costs.

## Decision

The change, as numbered claims. Each claim carries the alternative it beat and the reason.

**1. <Claim>**

Why this shape rather than the obvious one. Prefer arguments of the form "this makes `<bug class>`
unrepresentable" over "this fixes `<bug>`".

**2. <Claim>**

## Consequences

What becomes easier, what becomes harder, and what is simply now true. Include the migration story
for anything already deployed, and the default that keeps existing behaviour unchanged on day one.

## Alternatives considered

Each with the reason it was rejected. An alternative with no stated reason reads as one nobody thought
about, and invites the same debate again in six months.
