# F`<N>`-`<n>` — Todo

Spec: `docs/specs/YYYY-MM-DD-<topic>.md` · Plan: `tasks/<feature>/plan.md`

- [ ] T1 — `<scope>`
- [ ] T2 — `<scope>`
- [ ] T3 — `<scope>`
- [ ] T`<last>` — ADR-NNNN, docs, roadmap tick, `DEFERRED.md` rows, full gate

Each task: implement → focused checks → one independent risk-matched review → fix → commit.
